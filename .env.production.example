# ===========================================
# Production Environment Variables
# ===========================================
# Copy this file to .env on the SERVER at ~/motnha/motnhaerp/.env
# docker compose reads .env automatically for variable substitution
#
# cp .env.production.example .env
# nano .env  # edit values below

# PostgreSQL password (used by both postgres container and app)
POSTGRES_PASSWORD=change_me_to_a_secure_password

# NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=generate_me

# NextAuth.js URL (Tailscale IP of the server)
NEXTAUTH_URL=http://100.111.242.16:3000
