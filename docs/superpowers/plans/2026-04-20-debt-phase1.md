# Debt Phase 1 — Aging + Detail Drawer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm `dueDate` cho SupplierDebt/ContractorDebt + aging badge/filter + detail drawer gộp 3 section với inline payment popover.

**Architecture:** Schema thêm 1 field nullable. Client-side aging tính từ dueDate. UI có cột "Tình trạng" + filter aging trong list view. Click row → detail drawer ~60vw chứa 3 tab (Phiếu / Sổ cái / Lịch sử TT). Inline payment qua popover nhỏ.

**Tech Stack:** Next.js 16 App Router, Prisma 6, PostgreSQL, React 19, Zod 4.

---

## File Map

| File | Trách nhiệm |
|---|---|
| `prisma/schema.prisma` | + `dueDate DateTime?` vào SupplierDebt + ContractorDebt |
| `prisma/migrations/20260420090000_debt_due_date/migration.sql` | ALTER TABLE ADD COLUMN |
| `lib/validations/debt.js` | + `dueDate` optional vào 4 schema |
| `lib/debtAging.js` | **Mới** — utility `agingStatus(debt)` |
| `app/api/debts/supplier/route.js` | POST/GET pass-through dueDate |
| `app/api/debts/supplier/[id]/route.js` | PUT accept dueDate |
| `app/api/debts/contractor/route.js` | Same |
| `app/api/debts/contractor/[id]/route.js` | Same |
| `app/cong-no/page.js` | Cột "Tình trạng", filter aging, form thêm dueDate, detail drawer, inline payment popover |

---

## Task 1: Schema — dueDate vào Debt models

**Files:**
- Modify: `prisma/schema.prisma` (model SupplierDebt line ~2356, ContractorDebt line ~2400)
- Create: `prisma/migrations/20260420090000_debt_due_date/migration.sql`

- [ ] **Step 1: Thêm `dueDate` vào `SupplierDebt`**

Trong `model SupplierDebt`, thêm sau field `date`:
```prisma
  date        DateTime @default(now())
  dueDate     DateTime?
```

- [ ] **Step 2: Thêm `dueDate` vào `ContractorDebt`**

Trong `model ContractorDebt`, thêm sau field `date`:
```prisma
  date         DateTime @default(now())
  dueDate      DateTime?
```

- [ ] **Step 3: Tạo migration SQL**

Tạo `prisma/migrations/20260420090000_debt_due_date/migration.sql`:

```sql
ALTER TABLE "SupplierDebt" ADD COLUMN "dueDate" TIMESTAMP(3);
ALTER TABLE "ContractorDebt" ADD COLUMN "dueDate" TIMESTAMP(3);
```

- [ ] **Step 4: Apply migration**

```bash
cd d:/Codeapp/motnha && npx prisma migrate deploy
```

Expected: `Applied 1 migration: 20260420090000_debt_due_date`.

- [ ] **Step 5: Regen client**

```bash
cd d:/Codeapp/motnha && npm run db:generate
```

Expected: `✔ Generated Prisma Client`.

- [ ] **Step 6: Verify**

```bash
cd d:/Codeapp/motnha && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();(async()=>{const a=await p.supplierDebt.findMany({select:{id:true,dueDate:true},take:1});const b=await p.contractorDebt.findMany({select:{id:true,dueDate:true},take:1});console.log('Supplier sample:',a[0]);console.log('Contractor sample:',b[0]);await p.\$disconnect();})();"
```

Expected: 2 dòng in ra có `dueDate: null` (backward compatible).

- [ ] **Step 7: Commit**

```bash
cd d:/Codeapp/motnha && git add prisma/schema.prisma prisma/migrations/20260420090000_debt_due_date/
git commit -m "feat(schema): thêm dueDate vào SupplierDebt + ContractorDebt"
```

---

## Task 2: Validation — schema nhận dueDate

**Files:**
- Modify: `lib/validations/debt.js`

- [ ] **Step 1: Thêm `dueDate` vào `supplierDebtCreateSchema`**

File hiện tại có:
```javascript
export const supplierDebtCreateSchema = z.object({
    supplierId: z.string().min(1),
    projectId: z.string().optional().nullable().default(null),
    invoiceNo: optStr,
    description: z.string().min(1),
    totalAmount: z.number().positive(),
    date: optDate,
    proofUrl: optStr,
    notes: optStr,
}).strict();
```

Thêm field `dueDate: optDate` sau `date`:
```javascript
export const supplierDebtCreateSchema = z.object({
    supplierId: z.string().min(1),
    projectId: z.string().optional().nullable().default(null),
    invoiceNo: optStr,
    description: z.string().min(1),
    totalAmount: z.number().positive(),
    date: optDate,
    dueDate: optDate,
    proofUrl: optStr,
    notes: optStr,
}).strict();
```

- [ ] **Step 2: Thêm `dueDate` vào `supplierDebtUpdateSchema`**

File hiện tại có:
```javascript
export const supplierDebtUpdateSchema = z.object({
    invoiceNo: optStr.optional(),
    description: z.string().min(1).optional(),
    notes: optStr.optional(),
    proofUrl: optStr.optional(),
}).strict();
```

Thêm `dueDate: optDate.optional()`:
```javascript
export const supplierDebtUpdateSchema = z.object({
    invoiceNo: optStr.optional(),
    description: z.string().min(1).optional(),
    notes: optStr.optional(),
    proofUrl: optStr.optional(),
    dueDate: optDate.optional(),
}).strict();
```

- [ ] **Step 3: Thêm `dueDate` vào `contractorDebtCreateSchema` + `contractorDebtUpdateSchema`**

Tương tự 2 schema supplier ở trên. Sau `date` thêm `dueDate: optDate` cho create, sau `proofUrl` thêm `dueDate: optDate.optional()` cho update.

- [ ] **Step 4: Build check**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Expected: `✓ Compiled successfully`.

- [ ] **Step 5: Commit**

```bash
cd d:/Codeapp/motnha && git add lib/validations/debt.js
git commit -m "feat(validation): debt schemas accept dueDate"
```

---

## Task 3: API — POST/PUT persist dueDate

**Files:**
- Modify: `app/api/debts/supplier/route.js` (POST)
- Modify: `app/api/debts/supplier/[id]/route.js` (PUT)
- Modify: `app/api/debts/contractor/route.js` (POST)
- Modify: `app/api/debts/contractor/[id]/route.js` (PUT)

- [ ] **Step 1: Sửa POST supplier persist dueDate**

Trong POST handler của `app/api/debts/supplier/route.js`, tìm `data: { ...parsedData, code, createdById: ... }` hoặc pattern tương tự. Vì schema đã có `dueDate` optional, Prisma sẽ tự nhận qua spread. Chỉ cần confirm không có code chặn field khác.

Ví dụ nếu code hiện tại là:
```javascript
const data = supplierDebtCreateSchema.parse(body);
const code = await generateCode('supplierDebt', 'CN');
const debt = await prisma.supplierDebt.create({
    data: { code, ...data, createdById: session?.user?.id || '' },
});
```

→ Spread `...data` đã bao gồm `dueDate`. Không sửa gì thêm. **Chỉ verify code hiện tại có spread; nếu list thủ công từng field thì phải thêm `dueDate: data.dueDate || null`.**

- [ ] **Step 2: Sửa PUT supplier persist dueDate**

Trong PUT handler của `app/api/debts/supplier/[id]/route.js`, schema đã accept dueDate. Pattern tương tự — nếu spread thì OK, nếu list thủ công thì thêm `...(data.dueDate !== undefined && { dueDate: data.dueDate })`.

- [ ] **Step 3: Same cho contractor POST + PUT**

2 file: `app/api/debts/contractor/route.js` + `app/api/debts/contractor/[id]/route.js`. Confirm spread + accept dueDate.

- [ ] **Step 4: Test qua curl**

Chạy `npm run dev`. Lấy 1 supplierId từ DB:
```bash
curl -X POST http://localhost:3000/api/debts/supplier \
  -H "Content-Type: application/json" \
  -H "Cookie: <session>" \
  -d '{"supplierId":"<id>","description":"Test nợ có hạn","totalAmount":1000000,"dueDate":"2026-05-20"}'
```

Expected: Response 201 có `dueDate: "2026-05-20T00:00:00.000Z"`.

- [ ] **Step 5: Build check**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Expected: `✓ Compiled successfully`.

- [ ] **Step 6: Commit**

```bash
cd d:/Codeapp/motnha && git add app/api/debts/
git commit -m "feat(debt-api): persist dueDate on create + update"
```

---

## Task 4: Utility — `lib/debtAging.js`

**Files:**
- Create: `lib/debtAging.js`

- [ ] **Step 1: Tạo file**

```javascript
// Tính tình trạng (aging) của phiếu công nợ
export function agingStatus(debt) {
    const balance = (debt.totalAmount || 0) - (debt.paidAmount || 0);
    if (balance <= 0) return { label: 'Đã trả', color: 'success', days: 0, category: 'paid' };

    const today = new Date();
    today.setHours(0, 0, 0, 0);

    if (debt.dueDate) {
        const due = new Date(debt.dueDate);
        due.setHours(0, 0, 0, 0);
        const diffDays = Math.floor((today - due) / (1000 * 60 * 60 * 24));
        if (diffDays > 30) return { label: `Quá hạn ${diffDays} ngày`, color: 'danger', days: diffDays, category: 'overdue-30' };
        if (diffDays > 0) return { label: `Quá hạn ${diffDays} ngày`, color: 'warning', days: diffDays, category: 'overdue' };
        if (diffDays >= -7) return { label: `Sắp đến hạn (còn ${-diffDays}d)`, color: 'warning-light', days: diffDays, category: 'due-soon' };
        return { label: `Còn ${-diffDays} ngày`, color: 'neutral', days: diffDays, category: 'on-track' };
    }

    const created = new Date(debt.createdAt);
    created.setHours(0, 0, 0, 0);
    const ageDays = Math.floor((today - created) / (1000 * 60 * 60 * 24));
    return { label: `${ageDays} ngày tồn dư`, color: 'neutral', days: ageDays, category: 'no-due' };
}

// Filter helpers
export function matchesAgingFilter(debt, filter) {
    if (filter === 'all' || !filter) return true;
    const { category } = agingStatus(debt);
    if (filter === 'overdue-30') return category === 'overdue-30';
    if (filter === 'overdue') return category === 'overdue' || category === 'overdue-30';
    if (filter === 'due-soon') return category === 'due-soon';
    if (filter === 'on-track') return category === 'on-track' || category === 'no-due';
    return true;
}

// Badge style for the aging chip
export function agingBadgeStyle(color) {
    const map = {
        success: { background: 'rgba(34,197,94,0.15)', color: 'var(--status-success)' },
        danger: { background: 'rgba(239,68,68,0.2)', color: 'var(--status-danger)', fontWeight: 700 },
        warning: { background: 'rgba(245,158,11,0.2)', color: 'var(--status-warning)' },
        'warning-light': { background: 'rgba(234,179,8,0.15)', color: '#ca8a04' },
        neutral: { background: 'var(--bg-secondary)', color: 'var(--text-muted)' },
    };
    return map[color] || map.neutral;
}
```

- [ ] **Step 2: Build check**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Expected: `✓ Compiled successfully`.

- [ ] **Step 3: Commit**

```bash
cd d:/Codeapp/motnha && git add lib/debtAging.js
git commit -m "feat(lib): debtAging utility (status + filter + badge style)"
```

---

## Task 5: UI — Form tạo công nợ có dueDate

**Files:**
- Modify: `app/cong-no/page.js` (form state + modal render, ~line 753-795)

- [ ] **Step 1: Thêm `dueDate` vào form state**

Tìm state `debtForm` hoặc tương tự (khoảng line ~30-40). Thêm field `dueDate: ''`:

```javascript
const [debtForm, setDebtForm] = useState({
    description: '',
    invoiceNo: '',
    totalAmount: '',
    projectId: '',
    date: new Date().toISOString().split('T')[0],
    dueDate: '',
    notes: '',
    proofUrl: '',
});
```

- [ ] **Step 2: Thêm input `dueDate` trong modal tạo công nợ**

Trong modal render (khoảng line 753-795), tìm row có input `date`. Thêm input `dueDate` cạnh nó. Pattern:

```javascript
<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
    <div className="form-group">
        <label className="form-label">Ngày phát sinh</label>
        <input className="form-input" type="date" value={debtForm.date} onChange={e => setDebtForm({ ...debtForm, date: e.target.value })} />
    </div>
    <div className="form-group">
        <label className="form-label">Hạn trả (tuỳ chọn)</label>
        <input className="form-input" type="date" value={debtForm.dueDate} onChange={e => setDebtForm({ ...debtForm, dueDate: e.target.value })} />
    </div>
</div>
```

Nếu form hiện tại có `date` đứng 1 mình, wrap nó lại thành grid 2 cột để thêm dueDate cạnh.

- [ ] **Step 3: Gửi `dueDate` khi submit**

Tìm `handleCreateDebt` (~line 113-127). Trong payload gửi API, thêm `dueDate: debtForm.dueDate || null`. Ví dụ:

```javascript
const payload = {
    supplierId: ...,
    description: debtForm.description,
    invoiceNo: debtForm.invoiceNo || '',
    totalAmount: Number(debtForm.totalAmount),
    date: debtForm.date || null,
    dueDate: debtForm.dueDate || null,
    projectId: debtForm.projectId || null,
    notes: debtForm.notes || '',
    proofUrl: debtForm.proofUrl || '',
};
```

- [ ] **Step 4: Reset form include dueDate**

Khi đóng modal hoặc submit thành công, reset debtForm phải có `dueDate: ''`.

- [ ] **Step 5: Build + manual test**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

`npm run dev` → vào `/cong-no` → tab NCC → tạo công nợ mới với dueDate = 10 ngày trước hôm nay → submit → check DB qua `db:studio`: dueDate đúng.

- [ ] **Step 6: Commit**

```bash
cd d:/Codeapp/motnha && git add app/cong-no/page.js
git commit -m "feat(cong-no): form tạo công nợ có hạn trả (dueDate)"
```

---

## Task 6: UI — Cột Tình trạng + filter aging

**Files:**
- Modify: `app/cong-no/page.js`

- [ ] **Step 1: Import `agingStatus` và helpers**

Trên cùng file:
```javascript
import { agingStatus, matchesAgingFilter, agingBadgeStyle } from '@/lib/debtAging';
```

- [ ] **Step 2: Thêm state `agingFilter`**

Cạnh `debtFilterStatus`:
```javascript
const [agingFilter, setAgingFilter] = useState('all');
```

- [ ] **Step 3: Thêm dropdown Filter aging trong toolbar**

Tìm filter toolbar của tab NCC/Contractor (chỗ có `debtFilterStatus`). Thêm dropdown mới:

```javascript
<select className="form-select" value={agingFilter} onChange={e => setAgingFilter(e.target.value)} style={{ maxWidth: 200 }}>
    <option value="all">Tất cả tình trạng</option>
    <option value="overdue-30">🔴 Quá hạn &gt; 30 ngày</option>
    <option value="overdue">🟠 Quá hạn</option>
    <option value="due-soon">🟡 Sắp đến hạn (&lt; 7d)</option>
    <option value="on-track">⚪ Còn hạn / Chưa có hạn</option>
</select>
```

- [ ] **Step 4: Áp dụng aging filter vào list render**

Tìm đoạn `.filter()` render bảng phiếu (line ~366 hoặc tương tự). Thêm aging filter:

```javascript
{nccDebts.filter(d => {
    if (debtFilterStatus !== 'all' && d.status !== debtFilterStatus) return false;
    if (!matchesAgingFilter(d, agingFilter)) return false;
    return true;
}).map(d => { ... })}
```

- [ ] **Step 5: Thêm cột "Tình trạng" vào table header**

Tìm `<thead>` của bảng phiếu NCC. Thêm `<th>Tình trạng</th>` giữa "Còn nợ" và "TT":

```javascript
<tr>
    <th>Mã</th>
    <th>Mô tả / HĐ</th>
    <th>Dự án</th>
    <th>Ngày</th>
    <th>Hạn trả</th>
    <th style={{ textAlign: 'right' }}>Tổng</th>
    <th style={{ textAlign: 'right' }}>Đã trả</th>
    <th style={{ textAlign: 'right' }}>Còn nợ</th>
    <th>Tình trạng</th>
    <th>TT</th>
    <th></th>
</tr>
```

- [ ] **Step 6: Render cột "Hạn trả" + "Tình trạng" trong body**

Trong mỗi row render, trước cột "Tổng" thêm cột Hạn trả, và trước "TT" thêm cột Tình trạng:

```javascript
{debts.filter(...).map(d => {
    const aging = agingStatus(d);
    const style = agingBadgeStyle(aging.color);
    const isOverdue30 = aging.category === 'overdue-30';
    return (
        <tr key={d.id} style={{ background: isOverdue30 ? 'rgba(239,68,68,0.05)' : undefined, cursor: 'pointer' }} onClick={() => openDebtDetail(d)}>
            <td>{d.code}</td>
            <td>{d.description}{d.invoiceNo && <span style={{ opacity: 0.6, fontSize: 11, marginLeft: 6 }}>({d.invoiceNo})</span>}</td>
            <td>{d.project?.name || '—'}</td>
            <td>{fmtDate(d.date)}</td>
            <td>{d.dueDate ? fmtDate(d.dueDate) : '—'}</td>
            <td style={{ textAlign: 'right' }}>{fmt(d.totalAmount)}</td>
            <td style={{ textAlign: 'right', color: 'var(--status-success)' }}>{fmt(d.paidAmount)}</td>
            <td style={{ textAlign: 'right', fontWeight: 700, color: 'var(--status-warning)' }}>{fmt(d.totalAmount - d.paidAmount)}</td>
            <td>
                <span className="badge" style={{ ...style, padding: '2px 8px', fontSize: 11, whiteSpace: 'nowrap' }}>
                    {aging.label}
                </span>
            </td>
            <td>{/* status badge nguyên */}</td>
            <td>{/* action buttons nguyên */}</td>
        </tr>
    );
})}
```

> Note: `openDebtDetail(d)` sẽ tạo ở Task 7.

- [ ] **Step 7: Áp dụng cho Contractor tab**

Tương tự cho tab `contractor`. Cùng cột + filter.

- [ ] **Step 8: Build + test**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Run dev, vào /cong-no → tab NCC → thấy cột "Tình trạng" với badge màu. Filter dropdown chọn "Quá hạn > 30 ngày" → chỉ hiện phiếu quá hạn.

- [ ] **Step 9: Commit**

```bash
cd d:/Codeapp/motnha && git add app/cong-no/page.js
git commit -m "feat(cong-no): cột Tình trạng + filter aging cho NCC + Thầu phụ"
```

---

## Task 7: UI — Detail drawer gộp 3 section

**Files:**
- Modify: `app/cong-no/page.js`

- [ ] **Step 1: Thêm state cho drawer**

```javascript
const [detailDebt, setDetailDebt] = useState(null); // single debt data
const [detailSection, setDetailSection] = useState('debts'); // 'debts' | 'ledger' | 'payments'
const [paymentPopoverId, setPaymentPopoverId] = useState(null);
const [paymentAmount, setPaymentAmount] = useState('');
const [paymentDate, setPaymentDate] = useState('');
const [paymentNote, setPaymentNote] = useState('');
const [paymentSaving, setPaymentSaving] = useState(false);
const [supplierDebtsDetail, setSupplierDebtsDetail] = useState([]); // all debts of current supplier
const [ledgerData, setLedgerData] = useState(null);
const [paymentsData, setPaymentsData] = useState([]);
```

- [ ] **Step 2: Hàm `openDebtDetail(debt)` — load 3 section**

```javascript
const openDebtDetail = async (debt) => {
    setDetailDebt(debt);
    setDetailSection('debts');
    setPaymentPopoverId(null);

    const partnerType = activeTab === 'ncc' ? 'supplier' : 'contractor';
    const partnerId = activeTab === 'ncc' ? debt.supplierId : debt.contractorId;

    // Load 3 data sources
    const [debtsRes, ledgerRes] = await Promise.all([
        fetch(`/api/debts/${partnerType}?${partnerType}Id=${partnerId}`),
        fetch(`/api/debt/${partnerType === 'supplier' ? 'ncc' : 'contractors'}/${partnerId}/ledger`),
    ]);
    const debts = await debtsRes.json().catch(() => []);
    const ledger = await ledgerRes.json().catch(() => null);
    setSupplierDebtsDetail(Array.isArray(debts) ? debts : (debts?.data || []));
    setLedgerData(ledger);
    setPaymentsData((debts || []).flatMap(d => (d.payments || []).map(p => ({ ...p, debtCode: d.code }))));
};

const closeDebtDetail = () => {
    setDetailDebt(null);
    setPaymentPopoverId(null);
};
```

- [ ] **Step 3: Drawer JSX (render ở cuối component, cạnh các modal khác)**

```javascript
{detailDebt && (() => {
    const partnerName = activeTab === 'ncc' ? detailDebt.supplier?.name : detailDebt.contractor?.name;
    const totalBalance = supplierDebtsDetail.reduce((s, d) => s + (d.totalAmount - d.paidAmount), 0);
    const overdueCount = supplierDebtsDetail.filter(d => {
        const a = agingStatus(d);
        return a.category === 'overdue' || a.category === 'overdue-30';
    }).length;

    return (
        <div className="modal-overlay" onClick={closeDebtDetail} style={{ alignItems: 'stretch', justifyContent: 'flex-end' }}>
            <div className="modal" onClick={e => e.stopPropagation()} style={{
                width: '60vw', minWidth: 700, maxWidth: 1200, height: '100vh',
                borderRadius: 0, margin: 0, display: 'flex', flexDirection: 'column',
            }}>
                {/* Header */}
                <div className="modal-header" style={{ flexDirection: 'column', alignItems: 'flex-start', gap: 6 }}>
                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', width: '100%' }}>
                        <h3 style={{ margin: 0 }}>👤 {partnerName || '—'}</h3>
                        <button className="modal-close" onClick={closeDebtDetail}>×</button>
                    </div>
                    <div style={{ fontSize: 13, color: 'var(--text-muted)' }}>
                        💰 Số dư: <strong style={{ color: 'var(--status-warning)' }}>{fmt(totalBalance)}</strong>
                        {overdueCount > 0 && <> &nbsp;|&nbsp; 🔴 Quá hạn: <strong style={{ color: 'var(--status-danger)' }}>{overdueCount} phiếu</strong></>}
                    </div>
                </div>

                {/* Section tabs */}
                <div style={{ display: 'flex', gap: 0, borderBottom: '1px solid var(--border)' }}>
                    {[
                        { v: 'debts', label: '📋 Phiếu công nợ' },
                        { v: 'ledger', label: '📊 Sổ cái' },
                        { v: 'payments', label: '💳 Lịch sử TT' },
                    ].map(t => (
                        <button key={t.v}
                            onClick={() => setDetailSection(t.v)}
                            style={{
                                padding: '10px 16px', fontSize: 13, fontWeight: 600,
                                background: detailSection === t.v ? 'var(--bg-card)' : 'transparent',
                                borderBottom: detailSection === t.v ? '2px solid var(--primary)' : '2px solid transparent',
                                border: 'none', cursor: 'pointer',
                                color: detailSection === t.v ? 'var(--primary)' : 'var(--text-muted)',
                            }}>
                            {t.label}
                        </button>
                    ))}
                </div>

                {/* Section content */}
                <div style={{ flex: 1, overflowY: 'auto', padding: 16 }}>
                    {detailSection === 'debts' && (
                        <DebtsSection
                            debts={supplierDebtsDetail}
                            paymentPopoverId={paymentPopoverId}
                            setPaymentPopoverId={setPaymentPopoverId}
                            paymentAmount={paymentAmount}
                            setPaymentAmount={setPaymentAmount}
                            paymentDate={paymentDate}
                            setPaymentDate={setPaymentDate}
                            paymentNote={paymentNote}
                            setPaymentNote={setPaymentNote}
                            paymentSaving={paymentSaving}
                            onPay={submitInlinePayment}
                            partnerType={activeTab === 'ncc' ? 'supplier' : 'contractor'}
                        />
                    )}
                    {detailSection === 'ledger' && <LedgerSection data={ledgerData} />}
                    {detailSection === 'payments' && <PaymentsSection payments={paymentsData} />}
                </div>

                {/* Footer */}
                <div className="modal-footer">
                    <button className="btn btn-ghost btn-sm" disabled title="Sẽ có trong Phase 2">💵 Trả hàng loạt</button>
                    <button className="btn btn-ghost btn-sm" disabled title="Sẽ có trong Phase 3">📤 Xuất Excel</button>
                    <button className="btn btn-ghost btn-sm" disabled title="Sẽ có trong Phase 3">🖨️ In</button>
                    <div style={{ flex: 1 }} />
                    <button className="btn btn-primary" onClick={closeDebtDetail}>Đóng</button>
                </div>
            </div>
        </div>
    );
})()}
```

- [ ] **Step 4: Component `DebtsSection` (inline trong file hoặc ngoài)**

Ở cuối file (hoặc tách file riêng nếu muốn), thêm:

```javascript
function DebtsSection({ debts, paymentPopoverId, setPaymentPopoverId, paymentAmount, setPaymentAmount, paymentDate, setPaymentDate, paymentNote, setPaymentNote, paymentSaving, onPay, partnerType }) {
    return (
        <table className="data-table" style={{ fontSize: 12 }}>
            <thead>
                <tr>
                    <th>Mã</th>
                    <th>Mô tả</th>
                    <th>Ngày</th>
                    <th>Hạn trả</th>
                    <th style={{ textAlign: 'right' }}>Tổng</th>
                    <th style={{ textAlign: 'right' }}>Đã trả</th>
                    <th style={{ textAlign: 'right' }}>Còn nợ</th>
                    <th>Tình trạng</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {debts.map(d => {
                    const aging = agingStatus(d);
                    const style = agingBadgeStyle(aging.color);
                    const balance = d.totalAmount - d.paidAmount;
                    const canPay = balance > 0;
                    return (
                        <Fragment key={d.id}>
                            <tr>
                                <td>{d.code}</td>
                                <td>{d.description}</td>
                                <td>{new Date(d.date).toLocaleDateString('vi-VN')}</td>
                                <td>{d.dueDate ? new Date(d.dueDate).toLocaleDateString('vi-VN') : '—'}</td>
                                <td style={{ textAlign: 'right' }}>{new Intl.NumberFormat('vi-VN').format(d.totalAmount)}</td>
                                <td style={{ textAlign: 'right', color: 'var(--status-success)' }}>{new Intl.NumberFormat('vi-VN').format(d.paidAmount)}</td>
                                <td style={{ textAlign: 'right', fontWeight: 700, color: balance > 0 ? 'var(--status-warning)' : 'var(--text-muted)' }}>{new Intl.NumberFormat('vi-VN').format(balance)}</td>
                                <td><span className="badge" style={{ ...style, padding: '2px 8px', fontSize: 11 }}>{aging.label}</span></td>
                                <td>
                                    {canPay && (
                                        <button className="btn btn-ghost btn-sm" onClick={() => {
                                            setPaymentPopoverId(paymentPopoverId === d.id ? null : d.id);
                                            setPaymentAmount(String(balance));
                                            setPaymentDate(new Date().toISOString().split('T')[0]);
                                            setPaymentNote('');
                                        }}>💵 Trả</button>
                                    )}
                                </td>
                            </tr>
                            {paymentPopoverId === d.id && (
                                <tr>
                                    <td colSpan={9} style={{ background: 'var(--bg-secondary)', padding: 12 }}>
                                        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', alignItems: 'center' }}>
                                            <label style={{ fontSize: 12 }}>Số tiền:
                                                <input className="form-input" type="number" value={paymentAmount} onChange={e => setPaymentAmount(e.target.value)} style={{ width: 140, marginLeft: 6 }} />
                                            </label>
                                            <label style={{ fontSize: 12 }}>Ngày:
                                                <input className="form-input" type="date" value={paymentDate} onChange={e => setPaymentDate(e.target.value)} style={{ width: 140, marginLeft: 6 }} />
                                            </label>
                                            <label style={{ fontSize: 12, flex: 1, minWidth: 180 }}>Ghi chú:
                                                <input className="form-input" value={paymentNote} onChange={e => setPaymentNote(e.target.value)} style={{ marginLeft: 6, width: '80%' }} />
                                            </label>
                                            <button className="btn btn-ghost btn-sm" onClick={() => setPaymentPopoverId(null)}>Hủy</button>
                                            <button className="btn btn-primary btn-sm" disabled={paymentSaving} onClick={() => onPay(d.id, partnerType)}>
                                                {paymentSaving ? '...' : '✓ Xác nhận'}
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            )}
                        </Fragment>
                    );
                })}
                {debts.length === 0 && (
                    <tr><td colSpan={9} style={{ textAlign: 'center', color: 'var(--text-muted)', padding: 30 }}>Không có phiếu nào</td></tr>
                )}
            </tbody>
        </table>
    );
}

function LedgerSection({ data }) {
    if (!data) return <div style={{ padding: 20, color: 'var(--text-muted)' }}>Đang tải sổ cái...</div>;
    return (
        <div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))', gap: 8, marginBottom: 12 }}>
                <div className="stat-card"><div style={{ fontSize: 10, color: 'var(--text-muted)' }}>Đầu kỳ</div><div style={{ fontWeight: 700 }}>{new Intl.NumberFormat('vi-VN').format(data.summary?.openingBalance || 0)}</div></div>
                <div className="stat-card"><div style={{ fontSize: 10, color: 'var(--text-muted)' }}>Phát sinh</div><div style={{ fontWeight: 700, color: 'var(--status-warning)' }}>{new Intl.NumberFormat('vi-VN').format(data.summary?.totalDebit || 0)}</div></div>
                <div className="stat-card"><div style={{ fontSize: 10, color: 'var(--text-muted)' }}>Thanh toán</div><div style={{ fontWeight: 700, color: 'var(--status-success)' }}>{new Intl.NumberFormat('vi-VN').format(data.summary?.totalCredit || 0)}</div></div>
                <div className="stat-card"><div style={{ fontSize: 10, color: 'var(--text-muted)' }}>Cuối kỳ</div><div style={{ fontWeight: 700, color: 'var(--primary)' }}>{new Intl.NumberFormat('vi-VN').format(data.summary?.closingBalance || 0)}</div></div>
            </div>
            <table className="data-table" style={{ fontSize: 12 }}>
                <thead><tr><th>Ngày</th><th>Loại</th><th>Chứng từ</th><th>Dự án</th><th style={{ textAlign: 'right' }}>Phát sinh</th><th style={{ textAlign: 'right' }}>Thanh toán</th><th style={{ textAlign: 'right' }}>Số dư</th></tr></thead>
                <tbody>
                    {(data.entries || []).map((e, i) => (
                        <tr key={i}>
                            <td>{new Date(e.date).toLocaleDateString('vi-VN')}</td>
                            <td>{e.type}</td>
                            <td>{e.code || '—'}</td>
                            <td>{e.projectName || '—'}</td>
                            <td style={{ textAlign: 'right', color: 'var(--status-warning)' }}>{e.debit > 0 ? new Intl.NumberFormat('vi-VN').format(e.debit) : ''}</td>
                            <td style={{ textAlign: 'right', color: 'var(--status-success)' }}>{e.credit > 0 ? new Intl.NumberFormat('vi-VN').format(e.credit) : ''}</td>
                            <td style={{ textAlign: 'right', fontWeight: 600 }}>{new Intl.NumberFormat('vi-VN').format(e.balance || 0)}</td>
                        </tr>
                    ))}
                </tbody>
            </table>
        </div>
    );
}

function PaymentsSection({ payments }) {
    return (
        <table className="data-table" style={{ fontSize: 12 }}>
            <thead><tr><th>Mã TT</th><th>Phiếu công nợ</th><th>Ngày</th><th style={{ textAlign: 'right' }}>Số tiền</th><th>Ghi chú</th><th>Chứng từ</th></tr></thead>
            <tbody>
                {payments.map(p => (
                    <tr key={p.id}>
                        <td>{p.code}</td>
                        <td>{p.debtCode}</td>
                        <td>{new Date(p.date).toLocaleDateString('vi-VN')}</td>
                        <td style={{ textAlign: 'right', fontWeight: 600, color: 'var(--status-success)' }}>{new Intl.NumberFormat('vi-VN').format(p.amount)}</td>
                        <td>{p.notes || ''}</td>
                        <td>{p.proofUrl ? <a href={p.proofUrl} target="_blank" rel="noreferrer">🔗 Xem</a> : '—'}</td>
                    </tr>
                ))}
                {payments.length === 0 && <tr><td colSpan={6} style={{ textAlign: 'center', color: 'var(--text-muted)', padding: 20 }}>Chưa có thanh toán</td></tr>}
            </tbody>
        </table>
    );
}
```

Thêm import `Fragment` ở đầu file:
```javascript
import { useState, useEffect, Fragment } from 'react';
```

- [ ] **Step 5: Build check**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Expected: `✓ Compiled successfully` (Task 8 sẽ thêm `submitInlinePayment` — nếu lỗi runtime "not defined" tạm ignore).

- [ ] **Step 6: Commit**

```bash
cd d:/Codeapp/motnha && git add app/cong-no/page.js
git commit -m "feat(cong-no): detail drawer gộp 3 section (phiếu/sổ cái/lịch sử TT)"
```

---

## Task 8: UI — Inline payment popover logic

**Files:**
- Modify: `app/cong-no/page.js`

- [ ] **Step 1: Thêm function `submitInlinePayment`**

```javascript
const submitInlinePayment = async (debtId, partnerType) => {
    const amount = Number(paymentAmount);
    if (!amount || amount <= 0) return alert('Nhập số tiền hợp lệ');
    setPaymentSaving(true);
    try {
        const res = await fetch(`/api/debts/${partnerType}/${debtId}/pay`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                amount,
                date: paymentDate || new Date().toISOString().split('T')[0],
                notes: paymentNote || '',
                proofUrl: '',
            }),
        });
        if (!res.ok) {
            const e = await res.json();
            setPaymentSaving(false);
            return alert(e.error || 'Lỗi thanh toán');
        }
        setPaymentSaving(false);
        setPaymentPopoverId(null);
        // Reload drawer data + main list
        if (detailDebt) {
            await openDebtDetail(detailDebt);
        }
        if (activeTab === 'ncc') fetchNccDebts?.();
        if (activeTab === 'contractor') fetchContractorDebts?.();
    } catch (e) {
        setPaymentSaving(false);
        alert(e.message || 'Lỗi');
    }
};
```

> Note: `fetchNccDebts` / `fetchContractorDebts` là tên hàm load list trong file. Nếu tên khác, sửa lại cho đúng. Dùng optional chaining `?.()` cho an toàn.

- [ ] **Step 2: Build + manual test end-to-end**

```bash
cd d:/Codeapp/motnha && npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

Chạy `npm run dev`:
1. Vào `/cong-no` → tab NCC
2. Tạo 1 phiếu công nợ với dueDate = 10 ngày trước (cho test quá hạn)
3. List thấy badge đỏ "Quá hạn 10 ngày"
4. Filter "Quá hạn" → chỉ hiện phiếu này
5. Click row → drawer mở
6. Section "Phiếu công nợ" → click "💵 Trả" → popover mở, số tiền tự fill = còn nợ
7. Sửa số tiền nhỏ hơn → Xác nhận
8. Drawer refresh: phiếu giảm còn nợ, section "Lịch sử TT" có payment mới
9. Đóng drawer → list chính cũng refresh

- [ ] **Step 3: Commit**

```bash
cd d:/Codeapp/motnha && git add app/cong-no/page.js
git commit -m "feat(cong-no): inline payment popover trong drawer"
```

---

## Task 9: Smoke test E2E + push

- [ ] **Step 1: Full manual test**

Như Task 8 Step 2 nhưng rộng hơn:
1. **Create với dueDate** (NCC + Thầu phụ): verify DB persist
2. **Create không dueDate**: fallback "X ngày tồn dư", không cảnh báo đỏ
3. **Edit PUT dueDate**: sửa hạn muộn hơn → badge chuyển sang "chưa đến hạn"
4. **Filter aging**: 4 option hoạt động đúng
5. **Drawer 3 section**: mỗi tab load data đúng
6. **Inline payment**: trả 1 phần → còn dư → tiếp tục trả hết → status=paid, badge "Đã trả" xanh
7. **Refresh outside**: đóng drawer → list NCC refresh số liệu mới

- [ ] **Step 2: Final build**

```bash
cd d:/Codeapp/motnha && npm run build
```

Expected: Success.

- [ ] **Step 3: Push**

```bash
cd d:/Codeapp/motnha && git push origin main
```

- [ ] **Step 4: Fixup commit nếu có**

Nếu test phát hiện bug, fix + commit với prefix `fix(cong-no): ...` rồi push.
